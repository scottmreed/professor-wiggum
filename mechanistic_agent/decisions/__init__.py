"""Typed decision-model layer (PRD docs/PRD_jev_atom_identity_mechanistic.md §16.1).

``jev`` is a generic Decisions API client with no chemistry in it.
``policies`` resolves harness thresholds and policies. ``calibration`` holds
the metric functions used by ``scripts/calibrate_jev_reaction_type.py``.
"""
