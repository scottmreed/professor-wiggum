// Runtime configuration for the Mechanism Agent UI.
// Override API_BASE_URL to point the frontend at a remote API host.
// Leave empty (default) for same-origin local usage.
window.MECH_CONFIG = {
  API_BASE_URL: "",
};
