## Track 5: Single Reaction Submission

Save this file under `local_contributions/single_reactions/`. Do not commit it.

### Classification
- Outcome: `success` / `failure`
- Why it matters: `few_shot_candidate` / `subagent_gap` / `model_gap` / `harness_gap` / `validator_gap` / `other`

### Reaction
- Name:
- Starting materials:
- Expected products:
- Observed result:
- Reaction class, if known:

### Runtime details
- Run ID:
- Trace IDs:
- Model:
- Thinking level:
- Harness:
- Verified or unverified mode:

### What happened
- Short narrative:
- Why you believe this case deserves review:
- What change it might justify:

### Supporting artifacts
- Local screenshot path:
- Local trace path:
- Local fake PR draft path, if any:

### Suggested follow-up
- [ ] Consider as future few-shot candidate
- [ ] Consider as future subagent change
- [ ] Consider as future harness change
- [ ] Consider as future model comparison case

### Execution origin
- Execution channel: `direct_model` (hosted API key) or `agent-bridge` (keyless)
- If `agent-bridge` — responder kind: `orchestrator_subagents` / `cli` / `script` / `replay`
- Declared underlying model/agent:
- Budget observability: `full` (hosted, real cost) or `opaque` (agent-bridge)
- Official holdout exposed to the responder? `no`

### Contact info for possible manuscript attribution
- Name:
- Email:
- ORCID / GitHub / preferred handle:
- Contact before attribution? `yes` / `no`
