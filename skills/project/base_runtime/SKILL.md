# Base Runtime Skill

Defines shared runtime conventions for local mechanistic execution.

- Persist all run artifacts to SQLite.
- Emit structured events for every state transition.
- Keep deterministic checks explicit and auditable.
