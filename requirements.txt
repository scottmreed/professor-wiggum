pydantic>=2.6
typer>=0.12
python-dotenv>=1.0
fastapi>=0.115
uvicorn>=0.30
rich>=13.0.0
openai>=1.40.0
anthropic>=0.40.0
google-genai>=1.0.0
# Chemistry dependencies (required for harness functionality)
rdkit>=2022.9.5  # Via conda: conda install -c conda-forge rdkit
                # Via pip: pip install rdkit-pypi
                # Required for: atom counting, SMILES validation, mechanism validation
dimorphite-dl>=1.3.1
